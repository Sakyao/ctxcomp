# Requirements of the suite itself: reading methods.yml and writing one YAML config
# per row. That is the whole list.
pyyaml>=6

# ---------------------------------------------------------------------------
# NOT installed from here -- these belong to the harness this suite drives, and pin
# them where the baselines actually run (ACON_ROOT), so a version cannot drift
# between the two repositories:
#
#   appworld==0.1.3.post1        the environment and its evaluator
#   tiktoken                     Steps/Peak/Dep, via analysis_tools
#   productive_agents            ACON_ROOT/src (put it on PYTHONPATH)
#
# The LLMLingua rows additionally need the local HTTP service, from ACON:
#   python ACON_ROOT/experiments/repro/llmlingua_server.py --port 9999
# (llmlingua + torch; the CPU wheel is enough)
# ---------------------------------------------------------------------------
