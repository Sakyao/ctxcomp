# Prompt rendering. The only hard dependency of the harness proper.
jinja2>=3.1

# Token counting for Peak / Dep, so the numbers are recomputed from the stored
# trajectories rather than trusted from a counter written at run time.
tiktoken>=0.7

# Environment and its evaluator. Pins the benchmark, not the method.
appworld==0.1.3.post1

# Optional: plotting only.
# matplotlib>=3.7,<4
